{
  "name": "mywiki-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-data": "node scripts/prepare.js",
    "dev": "npm run prepare-data && vitepress dev",
    "build": "npm run prepare-data && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.0",
    "vue": "^3.5.0",
    "d3": "^7.9.0",
    "gray-matter": "^4.0.3"
  }
}
